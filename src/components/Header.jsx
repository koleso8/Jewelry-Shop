import Navigation from './Navigation';
import UserMenu from './UserMenu';

const Header = () => {
  return (
    <header>
      <Navigation />
      <UserMenu />
    </header>
  );
};

export default Header;
